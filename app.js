const calculator = {
  plus: function (a, b) {
    console.log(a + b);
  },
  minus: function (a, b) {
    console.log(a - b);
  },
  multiple: function (a, b) {
    console.log(a * b);
  },
  div: function (a, b) {
    console.log(a / b);
  },
};

calculator.plus(10, 2);
calculator.minus(10, 2);
calculator.multiple(10, 2);
calculator.div(10, 2);
